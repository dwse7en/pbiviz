import "./../style/visual.less";
import powerbi from "powerbi-visuals-api";
import VisualConstructorOptions = powerbi.extensibility.visual.VisualConstructorOptions;
import VisualUpdateOptions = powerbi.extensibility.visual.VisualUpdateOptions;
import IVisual = powerbi.extensibility.visual.IVisual;
export declare class Visual implements IVisual {
    private target;
    private host;
    private formattingSettings;
    private formattingSettingsService;
    private container;
    private startDateInput;
    private endDateInput;
    private headerInput;
    private clearIconButton;
    private resetIconButton;
    private _iconClearColor;
    private _iconResetColor;
    private readonly CLEAR_ICON_SVG;
    private readonly RESET_ICON_SVG;
    private currentTarget;
    private latestDataView;
    private initialMin;
    private initialMax;
    private isLocaleZH;
    private suppressDefaultRangeApply;
    private lastAppliedStartMeasure;
    private lastAppliedEndMeasure;
    private isUserCleared;
    constructor(options: VisualConstructorOptions);
    private initUI;
    private createButton;
    private createIconButton;
    update(options: VisualUpdateOptions): void;
    private hexToRgb;
    private applyFormatting;
    private applyFilter;
    private restoreFilterFromOptions;
    private clearFilter;
    private applyDefaultMeasures;
    private lastChanged;
    private onDateChange;
    private validateDates;
    private formatDate;
    getFormattingModel(): powerbi.visuals.FormattingModel;
}
